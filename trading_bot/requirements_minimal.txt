# Minimal requirements for Drift Protocol Trading Bot
# Install these step by step to avoid dependency conflicts

# Core numerical computing (must be first)
numpy>=1.26.2,<2.0.0

# Data manipulation
pandas>=2.1.0,<2.3.0

# Environment and utilities  
python-dotenv>=1.0.1
requests>=2.28.0
websockets>=11.0.0

# Date/time
pendulum>=3.0.0

# Drift Protocol and Solana (install these together)
driftpy==0.8.76
solders>=0.21.0,<0.27.0
anchorpy>=0.20.1,<0.22.0

# Technical analysis (install last)
# pandas-ta==0.3.14b0  # Uncomment after other packages work